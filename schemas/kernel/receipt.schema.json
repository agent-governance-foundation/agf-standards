{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-governance-foundation.github.io/agf-standards/schemas/kernel/receipt.schema.json",
  "title": "Receipt",
  "description": "The signed record correlating a Decision to what actually happened.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier."
    },
    "decision_ref": {
      "type": "string",
      "description": "id of exactly one Decision."
    },
    "attempted": {
      "type": "boolean",
      "description": "Whether execution was attempted after the Decision."
    },
    "outcome": {
      "type": "string",
      "enum": ["executed", "not_executed", "unknown"]
    },
    "completed_at": {
      "type": "integer",
      "description": "When the outcome was recorded (Unix seconds)."
    },
    "signer": {
      "type": "string",
      "description": "Actor id of the enforcement or observation point."
    },
    "signature": {
      "type": "string",
      "description": "Signature over the Receipt content."
    },
    "execution_validation_ref": {
      "type": "string",
      "description": "Optional id of the Execution Validation Record (Spec 30 §4, schemas/execution-validation-record.schema.json) that preceded this Receipt's dispatch, if the PEP performed that check. Not a kernel field — an optional Core-format correlation, absent when Execution-Time Authorization Validation (Spec 30) was not performed. As of signature_version 1.1 (Spec 07 §10.2), this field is inside the Receipt's signed payload; under 1.0 it was not — check signature_version before treating this correlation as cryptographically bound, not just present."
    }
  },
  "required": [
    "id",
    "decision_ref",
    "attempted",
    "outcome",
    "completed_at",
    "signer",
    "signature"
  ],
  "additionalProperties": true
}
